# GitHub Actions Unity activation file workflow
#
# Modified from: https://game.ci/docs/github/activation
# Modified: 2021 by Vivien Richter <vivien-richter@outlook.de>
# License: CC BY 4.0
name: Acquire Unity activation file 🔑

# Controls when the action will run.
on:
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:
    inputs:
      unityVersion:
        description: Unity version
        required: true

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  activation_file:
    name: Request manual activation file
    runs-on: ubuntu-latest
    steps:
      # Request manual activation file
      - name: Request manual activation file
        id: getManualLicenseFile
        uses: game-ci/unity-request-activation-file@v2.0-alpha-1
        with:
          unityVersion: ${{ github.event.inputs.unityVersion }}

      # Upload artifact (Unity_vXXXX.X.XXXX.alf)
      - name: Expose activation file as artifact
        uses: actions/upload-artifact@v2
        with:
          name: ${{ steps.getManualLicenseFile.outputs.filePath }}
          path: ${{ steps.getManualLicenseFile.outputs.filePath }}
